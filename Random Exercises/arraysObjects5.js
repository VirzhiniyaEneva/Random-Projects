/*
Create an array of 3 objects, each representing a fruit with name and color. Use a for loop to log each fruit's details.
 */

const fruits = [
    {
        name: "apple",
        color: "green",
    },
    {
        name: "orange",
        color: "orange",
    },
    {
        name: "pear",
        color: "Yellow",
    },
];
for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}
